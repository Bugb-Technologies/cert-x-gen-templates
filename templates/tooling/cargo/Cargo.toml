[package]
name = "redis-unauthenticated"
version = "1.0.0"
edition = "2021"

[[bin]]
name = "redis-unauthenticated"
path = "redis-unauthenticated.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }

"""BENIGN SYNTHETIC FIXTURE - the flawed twin's installed package.

Inert on import. The whole point of this twin is what its BUILD BACKEND does
while pip is installing it, not what the package does afterwards.
"""

VERSION = "1.0.0"

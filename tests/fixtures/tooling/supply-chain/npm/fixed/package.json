{
  "name": "cxg-fixture-installhook-fixed",
  "version": "1.0.0",
  "private": true,
  "description": "BENIGN synthetic fixture: the FIXED twin. Its postinstall hook is present and genuinely active - it spawns node and a shell - but every effect stays inside its own package directory, so the probe must refute.",
  "license": "Apache-2.0",
  "scripts": {
    "postinstall": "node postinstall.js"
  }
}
